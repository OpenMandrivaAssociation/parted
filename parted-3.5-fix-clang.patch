diff -Naur parted-3.5/lib/config.h.in parted-3.5.tpg/lib/config.h.in
--- parted-3.5/lib/config.h.in	2022-04-18 18:41:55.000000000 +0000
+++ parted-3.5.tpg/lib/config.h.in	2022-05-28 19:18:04.253489610 +0000
@@ -1772,7 +1772,7 @@
    _GL_ATTRIBUTE_DEALLOC_FREE declares that the function returns pointers that
    can be freed via 'free'; it can be used only after declaring 'free'.  */
 /* Applies to: functions.  Cannot be used on inline functions.  */
-#if _GL_GNUC_PREREQ (11, 0)
+#if _GL_GNUC_PREREQ (11, 0) && !defined(__clang__)
 # define _GL_ATTRIBUTE_DEALLOC(f, i) __attribute__ ((__malloc__ (f, i)))
 #else
 # define _GL_ATTRIBUTE_DEALLOC(f, i)
diff -Naur parted-3.5/lib/stdio.in.h parted-3.5.tpg/lib/stdio.in.h
--- parted-3.5/lib/stdio.in.h	2022-02-17 23:23:37.000000000 +0000
+++ parted-3.5.tpg/lib/stdio.in.h	2022-05-28 19:19:14.863156672 +0000
@@ -95,7 +95,7 @@
    that can be freed by passing them as the Ith argument to the
    function F.  */
 #ifndef _GL_ATTRIBUTE_DEALLOC
-# if __GNUC__ >= 11
+# if __GNUC__ >= 11 && !defined(__clang__)
 #  define _GL_ATTRIBUTE_DEALLOC(f, i) __attribute__ ((__malloc__ (f, i)))
 # else
 #  define _GL_ATTRIBUTE_DEALLOC(f, i)
diff -Naur parted-3.5/lib/stdlib.in.h parted-3.5.tpg/lib/stdlib.in.h
--- parted-3.5/lib/stdlib.in.h	2022-02-17 23:23:37.000000000 +0000
+++ parted-3.5.tpg/lib/stdlib.in.h	2022-05-28 19:19:45.353013421 +0000
@@ -103,7 +103,7 @@
    that can be freed by passing them as the Ith argument to the
    function F.  */
 #ifndef _GL_ATTRIBUTE_DEALLOC
-# if __GNUC__ >= 11
+# if __GNUC__ >= 11 && !defined(__clang__)
 #  define _GL_ATTRIBUTE_DEALLOC(f, i) __attribute__ ((__malloc__ (f, i)))
 # else
 #  define _GL_ATTRIBUTE_DEALLOC(f, i)
diff -Naur parted-3.5/lib/string.in.h parted-3.5.tpg/lib/string.in.h
--- parted-3.5/lib/string.in.h	2022-04-18 18:03:31.000000000 +0000
+++ parted-3.5.tpg/lib/string.in.h	2022-05-28 19:18:46.143291883 +0000
@@ -71,7 +71,7 @@
    that can be freed by passing them as the Ith argument to the
    function F.  */
 #ifndef _GL_ATTRIBUTE_DEALLOC
-# if __GNUC__ >= 11
+# if __GNUC__ >= 11 && !defined(__clang__)
 #  define _GL_ATTRIBUTE_DEALLOC(f, i) __attribute__ ((__malloc__ (f, i)))
 # else
 #  define _GL_ATTRIBUTE_DEALLOC(f, i)
diff -Naur parted-3.5/lib/wchar.in.h parted-3.5.tpg/lib/wchar.in.h
--- parted-3.5/lib/wchar.in.h	2022-04-18 18:03:31.000000000 +0000
+++ parted-3.5.tpg/lib/wchar.in.h	2022-05-28 19:20:49.132714665 +0000
@@ -88,7 +88,7 @@
    that can be freed by passing them as the Ith argument to the
    function F.  */
 #ifndef _GL_ATTRIBUTE_DEALLOC
-# if __GNUC__ >= 11
+# if __GNUC__ >= 11 && !defined(__clang__)
 #  define _GL_ATTRIBUTE_DEALLOC(f, i) __attribute__ ((__malloc__ (f, i)))
 # else
 #  define _GL_ATTRIBUTE_DEALLOC(f, i)
diff -Naur parted-3.5/m4/gnulib-common.m4 parted-3.5.tpg/m4/gnulib-common.m4
--- parted-3.5/m4/gnulib-common.m4	2022-03-23 22:19:20.000000000 +0000
+++ parted-3.5.tpg/m4/gnulib-common.m4	2022-05-28 19:17:39.213608112 +0000
@@ -178,7 +178,7 @@
    _GL_ATTRIBUTE_DEALLOC_FREE declares that the function returns pointers that
    can be freed via 'free'; it can be used only after declaring 'free'.  */
 /* Applies to: functions.  Cannot be used on inline functions.  */
-#if _GL_GNUC_PREREQ (11, 0)
+#if _GL_GNUC_PREREQ (11, 0) && !defined(__clang__)
 # define _GL_ATTRIBUTE_DEALLOC(f, i) __attribute__ ((__malloc__ (f, i)))
 #else
 # define _GL_ATTRIBUTE_DEALLOC(f, i)
